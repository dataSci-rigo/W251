#!/usr/bin/env
#encode decode
import base64
while True:
	fil=input() 
	with open("/tmp/source.gif", "rb") as imageFile:
	    str = base64.b64encode(imageFile.read())
	    print str
	#jpg_as_text = base64.b64encode(open("/tmp/face-159.jpg","rb").read())
	#publish.single(strMqttChannel+strMsg, jpg_as_text, hostname="mosquitto")
	f=open("/tmp/17proof3"+fil+".gif","w+")
	f.write(base64.decodestring(str))
	f.close()
